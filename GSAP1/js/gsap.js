// Square Animation
gsap.from(".chucked", {
    duration: 1,
    rotation: 720,
    ease: "bounce",
    stagger: 0.5,
    x: -500,
    y: -500,
});

gsap.from(".chucked2", {
    duration: 1,
    rotation: 720,
    ease: "bounce",
    stagger: 0.5,
    x: -500,
    y: -500,
});

gsap.from(".chucked3", {
    duration: 1,
    rotation: 720,
    ease: "bounce",
    stagger: 0.5,
    x: -500,
    y: -500,
});

gsap.from(".chucked4", {
    duration: 1,
    rotation: 720,
    ease: "bounce",
    stagger: 0.5,
    x: -500,
    y: -500,
});

gsap.from(".chucked5", {
    duration: 1,
    rotation: 720,
    ease: "bounce",
    stagger: 0.5,
    x: -500,
    y: -500,
});

gsap.from(".chucked6", {
    duration: 1,
    rotation: 720,
    ease: "bounce",
    stagger: 0.5,
    x: -500,
    y: -500,
});
